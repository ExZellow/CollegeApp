package com.example.collegeapp.ui.schedule

/*import com.tom_roush.pdfbox.text.PDFTextStripper
import kotlin.Throws
import com.tom_roush.pdfbox.text.TextPosition
import kotlin.jvm.JvmStatic
import com.example.collegeapp.ui.schedule.PrintTextLocations
import com.tom_roush.pdfbox.pdmodel.PDDocument
import java.io.*

class PrintTextLocations: PDFTextStripper() {

    @Throws(IOException::class)
    override fun writeString(string: String, textPositions: List<TextPosition>) {

        for (text in textPositions) {
            println(
                text.unicode + " String[" + text.xDirAdj + "," +
                        text.yDirAdj + " fs=" + text.fontSize + " xscale=" +
                        text.xScale + " height=" + text.heightDir + " space=" +
                        text.widthOfSpace + " width=" +
                        text.widthDirAdj + "]"
            )
        }
    }

}*/